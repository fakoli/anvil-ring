# Session end-state (2026-08-28, ~05:00 PDT)

## Verified state of the machine (the build host (arm64))
- anvil-ring: `~/workspace-work/projects/anvil-ring`, head `dde558f`, 0 uncommitted.
- 0 `anvil-ring` processes running; no listeners on 19xxx test ports.
- Verified by PRINTING rows (`ps -Ax -o pid=,lstart=,command= | grep … | grep -v grep`),
  not by `grep -c` — a count matched the shell wrapper and produced a false positive.

## What shipped tonight (all committed, all run at least once)
- 4 bug fixes pinned by tests: response-body lifetime (truncation), `decode` swallowing
  peer death, bare-LF integer-underflow panic, unguarded `detach` clobber.
- `cargo/scripts/live_stream_gate.py` — regenerates engine+hub+tether+caller; asserts
  no-truncation, head-first-from-engine, not-buffered. Ran green (4 events x 0.4 s).
- `cargo/scripts/soak_tunnel.py` — promoted from /tmp, self-locating, no user paths.
  Ran green: 1 authorization, 0 resets, 0 refusals, 0 dial failures → STEADY STATE.
- `docs/i6-kickoff.md` — cold-start pack incl. the measured I-6 failure signature.
- Untracked a debug script (`check_flush.py`) that had leaked into the repo.
- Re-verified invariant enforcement sites: I-5 generation-guarded detach, I-10
  two-layer loopback check, response path bounded at 64, command path NOT bounded.

## Deliberately NOT done
- I-6 (caller termination on tether death) left OPEN with its red test. Plan is
  probe-first: sink teardown → keepalive → bounded command channel → end callers.
  Not blind-patched at 05:00; it changes a shutdown path affecting every tenant.
- Nothing on the operator's daily-driver host (absolute boundary; hosts the fleet). n8n CVE is the user's.
- No amd64/musl build attempted — Mini is arm64 and there is no authorized amd64 host.

## Unverified / open
- `CVE-2026-55358` could not be resolved by two independent searches. Citable instead:
  CVE-2026-25049, expression-escape RCE, fixed n8n >= 1.123.17 (or >= 2.5.2 on 2.x).
  My earlier "1.121.0" was too low. A 1.x→2.x jump is a migration, not a patch.
- Whether fleet tethers redial often is still unknown; the in-repo soak measures an
  IDLE tunnel and cannot answer it. Do not read STEADY STATE as "reconnects are rare".

## Process-noise postmortem (mine, not a system fault)
~30 replayed notices of already-dead test processes. Root causes, all mine:
1. Stale binaries kept logging traces absent from src/; I initially reasoned from them.
2. `ps`/`lsof` were true while the harness's own registry still held exited entries,
   so "0 processes running" overstated what the evidence supported.
3. `grep -c` counted the `sh -c` wrapper → a fabricated "2 processes".
4. Answering each notice consumes a turn, which advanced the queue and prolonged it.
Skill updated with all four, each with its reason.
